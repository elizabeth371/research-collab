# 智溯 · 环境变量示例
# 复制为 .env 并填入真实值:  cp .env.example .env   (Windows: copy .env.example .env)
# 注意: .env 已被 .gitignore 忽略, 切勿提交真实密钥到 git。

# DeepSeek API Key (可选)
# 配置后启用: AI 写作水印注入 / 机器翻译回译攻击 / 多 Agent 编排
# 未配置时上述能力降级, 离线功能 (水印检测/溯源链/证据包/攻击矩阵) 不受影响。
LLM_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# 水印密钥 (可选, 安全敏感)
# 未配置时后端自动生成随机密钥并持久化到 backend/.secrets/watermark_key.bin
# (该目录已被 .gitignore 忽略)。生产环境或需要固定密钥复现水印时, 请显式配置:
#   生成方式: openssl rand -hex 32
# 支持十六进制串或任意 UTF-8 口令。
# WATERMARK_SECRET_KEY=

# 操作日志哈希链盐 (可选, 安全敏感)
# 未配置时自动生成并持久化到 backend/.secrets/hashchain_salt.txt。
# 注意: 更换盐值后, 既有溯源链历史日志的哈希将无法通过校验 (校验端点会判 invalid),
# 因此配置后请保持稳定, 不要随意更换。
# HASHCHAIN_SALT=
